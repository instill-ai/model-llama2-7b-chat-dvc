build:
  gpu: true
  python_version: "3.11"  # support only 3.11
  system_packages:
  python_packages:
    - torch==2.2.1
    - sentencepiece==0.1.99
    - transformers==4.36.2
    - protobuf==4.24.4
    - accelerate==0.25.0
# the format for repo is {user_id}/{model_id}
# make sure the model id matched what you created on console
repo: admin/llama2-7b-chat
